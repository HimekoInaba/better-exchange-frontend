import React from 'react';

import AppContainer from '../containers/AppContainer';

const Routes = (
  <HashRouter>
    <Route exact="/" component={AppContainer}>
    <IndexRoute component={}/>
    </Route>
  </HashRouter>
);

export default Routes;
